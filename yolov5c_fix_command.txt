# Fix for yolov5c to achieve similar performance to classify/
python train_classification_task.py \
    --data regurgitationV1/data.yaml \
    --epochs 300 \
    --batch-size 128 \
    --device auto \
    --weights yolov5s.pt \
    --hyp yolov5c_fix_final.yaml \
    --optimizer Adam \
    --name yolov5c_fixed

# Key fixes to match successful classify/ setup:
# 1. Larger batch size (128 instead of 32)
# 2. Adam optimizer instead of SGD
# 3. No frozen layers (remove --freeze)
# 4. Minimal data augmentation (like successful setup)
# 5. Detection losses already disabled (as you mentioned)

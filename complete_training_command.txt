# Complete fix for your 40% accuracy problem
python train_classification_task.py \
    --data regurgitationV1/data.yaml \
    --epochs 100 \
    --batch-size 16 \
    --device auto \
    --weights yolov5s.pt \
    --hyp fixed_hyp_complete.yaml \
    --optimizer Adam \
    --name fixed_classification_training

# Key fixes applied:
# 1. No frozen layers (removed --freeze)
# 2. Higher learning rate (0.01 instead of 0.001)
# 3. Adam optimizer instead of SGD
# 4. Data augmentation enabled
# 5. Classification-specific hyperparameters
# 6. Detection losses disabled for pure classification
